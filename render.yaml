services:
  - type: web
    name: media-downloader-backend
    env: docker
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_PATH
        value: python3
      - key: CORS_ORIGIN
        sync: false
